name: Release
on:
  workflow_run:
    workflows: [CI]
    branches: [main]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  attestations: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    # Only release when CI passes; skip semantic-release's own commits.
    # head_commit can be null on workflow_dispatch or some fork triggers.
    if: >-
      github.event.workflow_run.conclusion == 'success'
      && (github.event.workflow_run.head_commit == null
        || !contains(github.event.workflow_run.head_commit.message, 'chore(release):'))
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup uv
        uses: ./.github/actions/setup-uv

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Semantic Release Version
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: uv run semantic-release version

      - name: Semantic Release Publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: uv run semantic-release publish

      - name: Validate package metadata
        run: uvx twine check dist/*

      # To publish to PyPI using trusted publishing (OIDC):
      #   1. Create a PyPI account at https://pypi.org/account/register/
      #   2. Go to https://pypi.org/manage/account/publishing/
      #   3. Add a trusted publisher:
      #        Owner: <your-github-owner>
      #        Repository: <your-repo-name>
      #        Workflow name: release.yml
      #        Environment: (leave blank)
      #   4. Run init.sh with --pypi, or uncomment the steps below.
      #
      # PYPI-START
      # - name: Publish to PyPI
      #   uses: pypa/gh-action-pypi-publish@release/v1
      #   with:
      #     packages-dir: dist/
      #     attestations: true
      #
      # - name: Attest build provenance
      #   uses: actions/attest-build-provenance@v2
      #   with:
      #     subject-path: dist/*
      # PYPI-END
